{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for PromptMate windows",
  "windows": ["main", "manager", "capture"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
